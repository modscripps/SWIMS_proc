function [h,t]=textbox(x,y,dx,dy,str,units)%function [h,t]=textbox(x,y,dx,dy,str,units)%Place a string inside a box of size dx, dy centered at location x,y.%If units is 0 (default), specify normalized units relative to the gcf.  %If it is 1, specify data units.%Outputs are the axes and text handles.%I could not figure out how to set gca back to the previous; so it is left%with h the current axes.oldax=gca;if ~exist('units')	units=0;endif units==1	h=axes('position',[x-dx/2,y+dy/2,dx,dy]);else	if get(gca,'YDir') == 'normal'		h=sub_axis(gca,[x-dx/2,y-dy/2,dx,dy]);	else		h=sub_axis(gca,[x-dx/2,y+dy/2,dx,dy]);	endendset(h,'Box','on')set(h,'TickLength',[0 0])set(h,'XTick',[])set(h,'YTick',[])t=text(.5,.5,str);set(t,'FontWeight','Bold')set(t,'HorizontalAlignment','center')set(t,'FontSize',14)set(h,'layer','top')%axes(oldax)